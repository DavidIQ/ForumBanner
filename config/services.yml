services:
    davidiq.ForumBanner.listener:
        class: davidiq\ForumBanner\event\listener
        arguments:
            - @config
            - @template
            - @user
            - %core.php_ext%
            - %core.root_path%
        tags:
            - { name: event.listener }
